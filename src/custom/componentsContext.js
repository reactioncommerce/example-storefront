/**
 * Import your custom React components here and add them to the exported object.
 * They will then be available in the `components` property of all Reaction Design
 * System components, as well as any of your own components that you've wrapped
 * with the `withComponents` higher-order component.
 *
 * This file is for custom components only and is expected to be empty when you
 * begin. You can instead modify src/lib/theme/components.js directly, but
 * keeping this separation helps to avoid conflicts when you pull in new versions.
 */

export default {

};
