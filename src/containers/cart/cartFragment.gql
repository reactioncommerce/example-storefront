fragment CartCommon on Cart {
  _id
  items(first: 20, after: $itemsAfterCursor) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        optionTitle
        productSlug
        productType
        variantTitle
        quantity
      }
    }
  }
}
